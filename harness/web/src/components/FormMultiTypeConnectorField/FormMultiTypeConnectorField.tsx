import React from 'react'
import { Container } from '@harnessio/uicore'

export const defaultMultiTypeConnectorField = (props: any): React.ReactElement => {
  return <Container {...(props as any)} />
}
