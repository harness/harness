import React from 'react'
import { Container } from '@harness/uicore'
import css from './RepoCommits.module.scss'

export default function RepoCommits(): JSX.Element {
  return <Container className={css.main}>RepoCommits!</Container>
}
